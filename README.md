# vue-node-download

```bash

# install back end dependencies
npm install
node index.js

# install client side dependencies
cd client
npm install
npm run dev

# populate mongoDB
cd CodeGenerator
node index.js
```
